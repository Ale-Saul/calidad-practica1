define(["model/canvas", "model/images", "model/sounds"], 
    function(Canvas, Images, Sounds) {
        return {
            canvas: Canvas,
            images: Images,
            sounds: Sounds
        };
    }); 